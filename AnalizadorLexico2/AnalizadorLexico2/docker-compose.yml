version: "3.9"

services:
  compilador:
    image: eclipse-temurin:17-jdk-jammy
    container_name: compilador_dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
